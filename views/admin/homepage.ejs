<nav class="navbar navbar-default" role="navigation" ng-controller="NavController as ctrl">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" ng-click="ctrl.isCollapsed = !ctrl.isCollapsed">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="http://www.huskysoftware.com.ar" target="_blank" class="navbar-brand">MAG</a>
    </div>

    <div class="collapse navbar-collapse" collapse="ctrl.isCollapsed">
      <!-- Move this first navbar to websrc so it isnt visible when bootstraping -->
      <ul class="nav navbar-nav">
        <li ng-show="ctrl.auth.isAuthenticated()"><a ui-sref="products">Products</a></li>
        <li ng-show="ctrl.auth.isAuthenticated()"><a ui-sref="categories">Categories</a></li>
        <li ng-show="ctrl.auth.isAuthenticated()"><a ui-sref="users">Users</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li ng-hide="ctrl.auth.isAuthenticated()"><a ui-sref="anon.login">Login</a></li>
        <li ng-hide="ctrl.auth.isAuthenticated()"><a ui-sref="anon.register">Register</a></li>
        <li ng-show="ctrl.auth.isAuthenticated()"><p class="navbar-text">Hello, <strong>{{ctrl.user}}</strong></p></li>
        <li ng-show="ctrl.auth.isAuthenticated()"><a href="#" ng-click="ctrl.logout()">Logout</a></li>
      </ul>
    </div>
  </div>
</nav>
<toast></toast>
<div ui-view></div>
